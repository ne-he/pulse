[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]  # long strings in prompts / model cards

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
