"""Service 3 — API. FastAPI REST for snapshots/history + a WebSocket that
streams live observations, predictions, alerts and incident cards to the
dashboard. Also exposes the demo control endpoint (play/pause/seek/spike)."""
